<%= form_with(model: eintrag, local: true) do |form| %>
  <% if eintrag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(eintrag.errors.count, "error") %> prohibited this eintrag from being saved:</h2>

      <ul>
      <% eintrag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :kosten_traeger_id %>
    <%= form.text_field :kosten_traeger_id, id: :eintrag_kosten_traeger_id %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.number_field :duration, id: :eintrag_duration %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :eintrag_date %>
  </div>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment, id: :eintrag_comment %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :eintrag_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
